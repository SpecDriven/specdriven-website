---
// Hero section with sophisticated geometric design and technical authority
---

<section id="hero" class="relative bg-gradient-mesh overflow-hidden">
  <!-- Geometric grid pattern background -->
  <div class="absolute inset-0 pattern-grid opacity-30"></div>

  <!-- Floating geometric shapes -->
  <div class="absolute top-20 right-20 w-64 h-64 bg-gradient-primary opacity-10 rounded-full animate-float" style="animation-delay: 0s;"></div>
  <div class="absolute bottom-32 left-16 w-48 h-48 bg-gradient-emerald opacity-10 transform rotate-45 animate-float" style="animation-delay: 2s;"></div>
  <div class="absolute top-1/2 right-1/3 w-32 h-32 border-2 border-indigo-200 transform rotate-12 animate-pulse-slow"></div>

  <div class="relative max-w-7xl mx-auto container-padding section-padding">
    <div class="grid lg:grid-cols-12 gap-12 items-center">
      <!-- Hero Content -->
      <div class="lg:col-span-7 xl:col-span-6 space-y-8">
        <!-- Announcement Badge -->
        <div class="inline-flex items-center px-6 py-3 glass rounded-full border border-indigo-200 text-indigo-800 text-sm font-semibold animate-fade-in">
          <div class="w-8 h-8 bg-gradient-primary rounded-full flex items-center justify-center mr-3">
            <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2L13.09 8.26L20 9L13.09 9.74L12 16L10.91 9.74L4 9L10.91 8.26L12 2Z"/>
            </svg>
          </div>
          Transform specs into shipped features with AI
        </div>

        <!-- Main Headline -->
        <h1 class="text-display font-heading font-bold text-charcoal leading-tight animate-slide-up">
          Align
          <span class="text-gradient">Business, PM, Dev, and Test</span>
          around living specifications
        </h1>

        <!-- Subheadline -->
        <p class="text-xl text-gray-600 leading-relaxed max-w-2xl animate-slide-up text-balance">
          Implement spec-driven practices that bridge business requirements and technical execution—eliminating handoff friction and shipping delays.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row gap-4 animate-slide-up">
          <button
            class="btn-primary text-lg shadow-purple transform transition-all duration-300 hover:scale-105"
            data-calendly-source="hero-primary"
          >
            Schedule Free Assessment
          </button>
          <a
            href="#how-it-works"
            class="btn-secondary text-lg inline-flex items-center justify-center group"
          >
            See How It Works
            <svg class="w-5 h-5 ml-2 transform group-hover:translate-y-0.5 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
            </svg>
          </a>
        </div>

        <!-- Value Props -->
        <div class="grid grid-cols-3 gap-6 pt-4 animate-fade-in">
          <div class="text-center">
            <div class="icon-box-emerald mx-auto mb-3">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <h3 class="font-semibold text-charcoal mb-1">Strategy Preserved</h3>
            <p class="text-sm text-gray-600">Intent to implementation</p>
          </div>
          <div class="text-center">
            <div class="icon-box-primary mx-auto mb-3">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
              </svg>
            </div>
            <h3 class="font-semibold text-charcoal mb-1">AI-Powered</h3>
            <p class="text-sm text-gray-600">Guardrailed automation</p>
          </div>
          <div class="text-center">
            <div class="icon-box-amber mx-auto mb-3">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
            </div>
            <h3 class="font-semibold text-charcoal mb-1">Faster Delivery</h3>
            <p class="text-sm text-gray-600">Reduced handoff delays</p>
          </div>
        </div>
      </div>

      <!-- Hero Visual -->
      <div class="lg:col-span-5 xl:col-span-6">
        <div class="relative animate-fade-in">
          <!-- Main spec document -->
          <div class="glass rounded-xl p-8 border-2 border-indigo-100 shadow-2xl">
            <!-- Header -->
            <div class="flex items-center justify-between mb-8">
              <div class="flex items-center space-x-4">
                <div class="w-10 h-10 bg-gradient-primary rounded-lg flex items-center justify-center">
                  <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M14 2H6C4.9 2 4 2.9 4 4V20C4 21.1 4.9 22 6 22H18C19.1 22 20 21.1 20 20V8L14 2Z"/>
                  </svg>
                </div>
                <div>
                  <div class="font-heading font-semibold text-charcoal text-lg">User Story Spec</div>
                  <div class="text-sm text-gray-500 font-mono">living_documentation.spec</div>
                </div>
              </div>
              <div class="flex items-center space-x-2">
                <div class="w-3 h-3 bg-emerald-500 rounded-full animate-pulse"></div>
                <span class="text-sm text-gray-600 font-medium">Synced</span>
              </div>
            </div>

            <!-- BDD Spec Content -->
            <div class="space-y-4">
              <div class="bg-gradient-to-r from-gray-50 to-gray-100 rounded-lg p-5 border border-gray-200">
                <div class="font-mono text-sm font-semibold text-gray-800 mb-2">GIVEN</div>
                <div class="font-mono text-sm text-gray-700">user is on the checkout page</div>
              </div>
              <div class="bg-gradient-to-r from-indigo-50 to-indigo-100 rounded-lg p-5 border border-indigo-200">
                <div class="font-mono text-sm font-semibold text-indigo-800 mb-2">WHEN</div>
                <div class="font-mono text-sm text-indigo-700">they apply a valid coupon code</div>
              </div>
              <div class="bg-gradient-to-r from-emerald-50 to-emerald-100 rounded-lg p-5 border border-emerald-200">
                <div class="font-mono text-sm font-semibold text-emerald-800 mb-2">THEN</div>
                <div class="font-mono text-sm text-emerald-700">discount is applied automatically</div>
              </div>
            </div>

            <!-- Transformation Arrow -->
            <div class="flex items-center justify-center my-6">
              <div class="flex items-center space-x-3">
                <div class="h-px w-8 bg-gradient-primary"></div>
                <div class="w-8 h-8 bg-gradient-primary rounded-full flex items-center justify-center">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"/>
                  </svg>
                </div>
                <div class="h-px w-8 bg-gradient-primary"></div>
              </div>
            </div>

            <!-- Generated Code -->
            <div class="bg-gradient-dark rounded-lg p-5 border border-gray-700">
              <div class="flex items-center justify-between mb-3">
                <div class="text-emerald-400 text-xs font-mono font-semibold">// AI-Generated Test</div>
                <div class="flex space-x-1">
                  <div class="w-2 h-2 bg-rose-400 rounded-full"></div>
                  <div class="w-2 h-2 bg-amber-400 rounded-full"></div>
                  <div class="w-2 h-2 bg-emerald-400 rounded-full"></div>
                </div>
              </div>
              <div class="font-mono text-sm text-white space-y-1">
                <div><span class="text-purple-400">test</span>(<span class="text-yellow-300">'applies coupon discount'</span>, <span class="text-blue-400">async</span> () => &#123;</div>
                <div class="pl-4 text-gray-300">// Test implementation...</div>
                <div>&#125;);</div>
              </div>
            </div>
          </div>

          <!-- Floating Badges -->
          <div class="absolute -top-6 -right-6 w-16 h-16 bg-gradient-primary rounded-xl flex items-center justify-center shadow-purple rotate-12 animate-float">
            <svg class="w-8 h-8 text-white" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 2L13.09 8.26L20 9L13.09 9.74L12 16L10.91 9.74L4 9L10.91 8.26L12 2Z"/>
            </svg>
          </div>

          <div class="absolute -bottom-6 -left-6 w-14 h-14 bg-gradient-emerald rounded-xl flex items-center justify-center shadow-lg -rotate-12 animate-float" style="animation-delay: 1s;">
            <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
              <path d="M9 12L11 14L15 10M21 12C21 16.97 16.97 21 12 21C7.03 21 3 16.97 3 12C3 7.03 7.03 3 12 3C16.97 3 21 7.03 21 12Z"/>
            </svg>
          </div>

          <!-- Corner accent -->
          <div class="absolute top-0 right-0 w-24 h-24 bg-gradient-primary opacity-5 geometric-corner"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { handleCalendlyClick, preloadCalendlyScript } from '../lib/calendly';

  // Handle hero CTA clicks
  const heroCtaButtons = document.querySelectorAll('[data-calendly-source^="hero"]');
  heroCtaButtons.forEach(button => {
    const source = button.getAttribute('data-calendly-source') || 'hero';

    // Preload script on hover/focus
    button.addEventListener('mouseenter', preloadCalendlyScript);
    button.addEventListener('focus', preloadCalendlyScript);

    // Handle click
    button.addEventListener('click', (e) => {
      e.preventDefault();
      handleCalendlyClick(source);
    });
  });

  // Add intersection observer for animations
  if (typeof window !== 'undefined') {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('animate-fade-in');
          }
        });
      },
      { threshold: 0.1 }
    );

    // Observe animated elements
    document.querySelectorAll('.animate-slide-up, .animate-fade-in').forEach((el) => {
      observer.observe(el);
    });
  }
</script>